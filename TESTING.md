# Takoyaki CMS 手動テスト手順書

対象バージョン: **v2.0.0**（ポートフォリオCMS）
最終更新: 2026-07-28

Takoyaki CMS の全機能をブラウザ上で一通り動作確認するためのチェックリストです。
上から順に実施することを想定しています（後半のテストが前半で作ったデータに依存します）。

> **v1.x からの変更点**
> テーマ / プラグイン / RSS / sitemap / きれいなURL / ゴミ箱 / 監査ログ /
> DBバックアップ / 複数ユーザー / パスワードリセット / カスタムフィールドは
> **v2.0.0 で削除されました**。旧手順書のこれらの章は丸ごと無くなっています。
> 代わりに **作品メタ**（3章）・**本文セクション**（4章）・**スキル**（10章）が加わりました。

---

## 目次

| 章 | 内容 | 所要 |
|----|------|------|
| [0](#0-前提準備) | 前提・準備 | 5分 |
| [1](#1-認証) | 認証（ログイン / レート制限） | 10分 |
| [2](#2-作品の作成) | 作品の作成（基本情報・作品メタ） | 15分 |
| [3](#3-作品メタと動画埋め込み) | 作品メタと動画埋め込み | 10分 |
| [4](#4-本文セクション) | 本文セクション（追加・削除・並び替え） | 15分 |
| [5](#5-カテゴリとタグの紐付け) | カテゴリとタグの紐付け | 10分 |
| [6](#6-予約公開プレビュー公開判定) | 予約公開・プレビュー・公開判定 | 15分 |
| [7](#7-作品一覧の操作) | 作品一覧の操作（検索・並び替え・削除） | 15分 |
| [8](#8-カテゴリ管理) | カテゴリ管理 | 10分 |
| [9](#9-タグ管理) | タグ管理 | 5分 |
| [10](#10-スキル管理) | スキル管理 | 15分 |
| [11](#11-メディア画像処理) | メディア・画像処理 | 15分 |
| [12](#12-サイト設定) | サイト設定 | 5分 |
| [13](#13-アカウント設定) | アカウント設定 | 10分 |
| [14](#14-公開ページ) | 公開ページ（Works / Skills） | 15分 |
| [15](#15-セキュリティ確認) | セキュリティ確認 | 20分 |
| [16](#16-v1x-からの移行確認) | v1.x からの移行確認 | 20分 |
| [付録C](#付録c-回帰テストv200-で直した不具合) | **回帰テスト（v2.0.0 で直した不具合）** | 20分 |

**時間が無いときは 0 → 2 → 4 → 10 → 14 → 付録C** だけでも実施してください。
この6つで主要機能とv2.0.0の修正内容をカバーできます。

---

## 0. 前提・準備

### 0.1 動作環境

| 項目 | 値 |
|------|-----|
| Webサーバ | MAMP Apache（ポート **8888**） |
| DB | MAMP MySQL（ポート **8889**） |
| ベースURL | `http://localhost:8888/takoyaki-cms` |
| 管理画面 | `http://localhost:8888/takoyaki-cms/admin/index.php` |
| DB名 | `takoyaki_test`（user: `root` / pass: `root`） |

> 以降、URLは `{BASE}` = `http://localhost:8888/takoyaki-cms` と表記します。

### 0.2 事前チェック

- [ ] MAMP が起動しており、Apache と MySQL が緑（稼働中）になっている
- [ ] `{BASE}/login.php` を開くとログイン画面が表示される
- [ ] `{BASE}/samples/works.php` を開くと作品一覧（公開ページ）が表示される
- [ ] `{BASE}/samples/skill.php` を開くとスキルページが表示される
- [ ] `config.php` が存在する（無ければ `cp config.example.php config.php` して編集）

### 0.3 CLI から DB を見る場合

MAMP の MySQL クライアントは通常の `mysql` コマンドとは別の場所にあります。

```bash
MYSQL=/Applications/MAMP/Library/bin/mysql80/bin/mysql

# 方法A: TCP 接続
$MYSQL -h 127.0.0.1 -P 8889 -uroot -proot takoyaki_test

# 方法B: MAMP のソケット経由
$MYSQL --socket=/Applications/MAMP/tmp/mysql/mysql.sock -uroot -proot takoyaki_test
```

PHP CLI から接続する場合も同様に、DSN で `host=127.0.0.1;port=8889` を指定するか、
`php -d pdo_mysql.default_socket=/Applications/MAMP/tmp/mysql/mysql.sock` を付けます。

### 0.4 テスト用アカウント

- 管理者: `admin`（パスワードは `setup.php` 登録時に設定したもの）
- **v2.0.0 は管理者1人構成です。** ロール（admin / editor）はありません

### 0.5 テストの記録方法

各項目の `[ ]` を実施したら `[x]` に変更し、期待と異なる場合は末尾にメモを残してください。

---

## 1. 認証

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 1-1 | `{BASE}/login.php` を開き、誤ったパスワードでログイン | 「ユーザー名またはパスワードが正しくありません」 | [ ] |
| 1-2 | **存在しないユーザー名**でログイン | 1-1 と**同じ**メッセージ（ユーザーの存在を明かさない） | [ ] |
| 1-3 | 正しい資格情報でログイン | `{BASE}/admin/index.php`（作品一覧）に遷移 | [ ] |
| 1-4 | ログイン状態で `{BASE}/login.php` を開く | 管理画面へリダイレクトされる（再ログインを求められない） | [ ] |
| 1-5 | 右上「ログアウト」をクリック | ログイン画面に戻る | [ ] |
| 1-6 | ログアウト状態で `{BASE}/admin/index.php` を直接開く | `login.php` にリダイレクトされる | [ ] |

### 1.7 ログイン試行回数制限

- [ ] ログアウト状態で、**わざと5回**連続で誤パスワードを送信する
- [ ] 6回目は「ログイン試行回数が多すぎます」でブロックされる（15分間）
- [ ] 制限中は**正しいパスワードでも**ログインできないことを確認

制限を解除してテストを続ける場合:

```sql
DELETE FROM login_attempts;
```

- [ ] ログイン成功後、`login_attempts` から**1日以上前の行が消えている**
      （v2.0.0 で追加した掃除処理。行が溜まり続けないこと）

---

## 2. 作品の作成

`{BASE}/admin/post-new.php`（作品一覧の「+ 新規作成」）で実施します。

### 2.1 基本情報

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 2-1 | タイトルだけ入力して保存 | 作品一覧に追加される | [ ] |
| 2-2 | タイトルを空のまま保存 | 「タイトルは必須です。」で保存されない | [ ] |
| 2-3 | **英字タイトル**（例 `My Portfolio Site`）で slug 欄を空のまま保存 | slug が `my-portfolio-site` になる | [ ] |
| 2-4 | **日本語のみのタイトル**（例 `ポートフォリオサイト`）で保存 | slug が `work-{ID}` になる（NULL にならない） | [ ] |
| 2-5 | 2-3 と**同じタイトル**でもう1件保存 | slug が `my-portfolio-site-2` になる（重複エラーにならない） | [ ] |
| 2-6 | slug 欄に手動で `custom-slug` と入力して保存 | その slug が使われる | [ ] |
| 2-7 | 概要（excerpt）を入力して保存 | 保存され、一覧カードに表示される | [ ] |
| 2-8 | サムネイル画像をアップロードして保存 | 編集画面にプレビューが表示される | [ ] |

> **2-4 / 2-5 は v1.x で壊れていた箇所です**（B-4）。
> 旧版では日本語タイトルの slug が NULL になり、同名タイトルの2件目は保存自体が失敗しました。

### 2.2 編集・削除

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 2-9 | 一覧からタイトルをクリックして編集画面を開く | 保存済みの内容がすべて復元されている | [ ] |
| 2-10 | サムネイルを別画像に差し替えて保存 | 新画像に変わり、**古い画像と `-thumb` 変種が `uploads/` から消える** | [ ] |
| 2-11 | 「この画像を削除する」にチェックして保存 | サムネイルが外れ、ファイルも削除される | [ ] |
| 2-12 | 一覧の「削除」をクリック | 確認ダイアログ →「削除」で完全に消える（**ゴミ箱はありません**） | [ ] |
| 2-13 | 2-12 で削除した作品のサムネイル画像 | `uploads/` からも削除されている | [ ] |

> **2-13 は v1.x で漏れていた箇所です**（B-7）。旧版では記事を消しても画像が残り続けました。

---

## 3. 作品メタと動画埋め込み

v2.0.0 で復活したフィールド群です。

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 3-1 | 「制作期間」に `2026.06 – 08` を入力して保存 | 一覧と公開ページに表示される | [ ] |
| 3-2 | 「種別」に `個人制作` を入力して保存 | 一覧と公開ページに表示される | [ ] |
| 3-3 | 「外部リンク」に `https://example.com` を入力して保存 | 公開ページに「サイトを見る ↗」が出る | [ ] |
| 3-4 | 「外部リンク」に `not-a-url` を入力して保存 | 「外部リンクのURLが正しくありません。」で拒否される | [ ] |

### 3.5 動画URL

以下の形式がすべて受け付けられることを確認します。

| # | 入力するURL | 期待される結果 | 確認 |
|---|------------|----------------|------|
| 3-5 | `https://www.youtube.com/watch?v=dQw4w9WgXcQ` | 保存でき、公開ページに埋め込み再生プレイヤーが出る | [ ] |
| 3-6 | `https://youtu.be/dQw4w9WgXcQ` | 同上 | [ ] |
| 3-7 | `https://www.youtube.com/shorts/dQw4w9WgXcQ` | 同上 | [ ] |
| 3-8 | `https://vimeo.com/123456789` | 同上（Vimeo プレイヤー） | [ ] |
| 3-9 | `https://example.com/movie.mp4` | 「YouTube または Vimeo のURLを入力してください。」で拒否 | [ ] |

- [ ] **サムネイル画像と動画URLの両方**を設定した作品の公開ページで、
      **動画が優先して表示される**（サムネイルは出ない）
- [ ] 動画プレイヤーが 16:9 でレスポンシブに縮む（ウィンドウ幅を狭めて確認）

---

## 4. 本文セクション

v2.0.0 の中心機能です。`admin/post-new.php` / `post-edit.php` の「本文セクション」欄で実施します。

### 4.1 基本操作

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 4-1 | 新規作成画面を開く | セクションが1行だけあり、番号「1」が付いている | [ ] |
| 4-2 | 見出しに `制作の背景`、本文を CKEditor で入力 | リッチテキスト（太字・見出し・リスト）が反映される | [ ] |
| 4-3 | 「+ セクションを追加」をクリック | 2行目が追加され、番号が「2」になり、その行にも CKEditor が付く | [ ] |
| 4-4 | セクションを3つまで増やして保存 | 3つとも保存され、編集画面を開き直しても順序が保たれる | [ ] |
| 4-5 | 見出しだけ入力して本文を空で保存 | 保存される（見出しのみのセクションは有効） | [ ] |
| 4-6 | 本文だけ入力して見出しを空で保存 | 保存される（公開ページでは見出しなしで本文だけ出る） | [ ] |
| 4-7 | **見出しも本文も空**の行を作って保存 | その行は保存されない（DBに行が増えない） | [ ] |

### 4.2 並び替え・削除

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 4-8 | 2行目の ↑ をクリック | 1行目と入れ替わり、**入力内容もCKEditorの中身も保持される** | [ ] |
| 4-9 | 1行目の ↓ をクリック | 2行目と入れ替わる | [ ] |
| 4-10 | 一番上の行の ↑ をクリック | 何も起きない（先頭より上には行かない） | [ ] |
| 4-11 | 一番下の行の ↓ をクリック | 何も起きない | [ ] |
| 4-12 | 並び替えた状態で保存 → 編集画面を開き直す | 並び替えた順序で保存されている | [ ] |
| 4-13 | 2行目の「削除」をクリック | 確認ダイアログ → その行だけ消え、番号が振り直される | [ ] |
| 4-14 | セクションを1行だけにする | **削除ボタンが消える**（0行にはできない） | [ ] |

> 4-8 は特に注意して確認してください。CKEditor は DOM を動かすと壊れるため、
> 内部でエディタを破棄 → 移動 → 再生成しています。**内容が消えたら不具合です。**

### 4.3 本文内画像アップロード

- [ ] CKEditor の画像ボタンから画像をアップロード
- [ ] 本文中に画像が挿入され、`uploads/` にファイルが保存される
- [ ] 保存 → 公開ページで画像が表示される
- [ ] 挿入した画像がメディアライブラリで「本文」として**使用中**と表示される

---

## 5. カテゴリとタグの紐付け

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 5-1 | 作品編集画面の「カテゴリー」欄 | **チェックボックス**で表示される（プルダウンではない） | [ ] |
| 5-2 | カテゴリを**2つ以上**チェックして保存 | 両方が保存され、編集画面を開き直してもチェックが残る | [ ] |
| 5-3 | 全部のチェックを外して保存 | カテゴリ紐付けが空になる | [ ] |
| 5-4 | カテゴリが1件も無い状態で作品編集画面を開く | 「カテゴリがまだありません」とカテゴリ管理へのリンクが出る | [ ] |
| 5-5 | タグ欄に `PHP, MySQL, SCSS` と入力して保存 | 未登録のタグが自動作成される | [ ] |
| 5-6 | 別の作品に `PHP` を付与 | 既存タグが再利用される（`admin/tags.php` で重複していない） | [ ] |
| 5-7 | タグ欄を空にして保存 | その作品のタグが全部外れる（タグ自体は残る） | [ ] |
| 5-8 | タグに日本語（例 `ポートフォリオ`）を入力して保存 | 作成され、slug は `tag` 系の代替名になる | [ ] |

> **5-1 / 5-2 は v1.x で単一選択しかできなかった箇所です**（C-2）。
> DBは多対多なのにUIがプルダウンで、1つしか選べませんでした。

---

## 6. 予約公開・プレビュー・公開判定

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 6-1 | ステータス「公開」・公開日時を**未来**にして保存 | 一覧で「予約」バッジ＋日時が表示される | [ ] |
| 6-2 | その作品を `{BASE}/samples/single.php?id={ID}` で開く | **404**（まだ公開時刻前） | [ ] |
| 6-3 | 公開日時を**過去**に変更して保存 | 一覧で「公開中」バッジになる | [ ] |
| 6-4 | 同じ作品を公開ページで開く | **200** で表示される | [ ] |
| 6-5 | ステータス「公開」・公開日時を**空**にして保存 | 保存時刻が published_at に入り、即「公開中」になる | [ ] |
| 6-6 | 下書きの作品を公開ページで開く | **404** | [ ] |
| 6-7 | 下書きを `{BASE}/preview.php?id={ID}` で開く（ログイン中） | 実際の見た目でプレビューされ、上部に「下書き」バッジが出る | [ ] |
| 6-8 | 予約公開の作品をプレビュー | 上部に「予約公開（日時）」バッジが出る | [ ] |
| 6-9 | ログアウトして `preview.php?id=...` を開く | `login.php` にリダイレクト | [ ] |
| 6-10 | プレビュー画面で動画・セクション・作品メタが表示される | 公開ページとほぼ同じ内容が出る | [ ] |

### 6.11 タイムゾーンの一致確認（重要）

v1.x では PHP と MySQL のタイムゾーンがずれ、管理画面の表示と実際の公開状態が
食い違うことがありました（B-5）。

- [ ] 公開日時を**現在時刻の1〜2分後**に設定して保存する
- [ ] 一覧で「予約」バッジが出ている（「公開中」になっていない）
- [ ] 公開ページを開くと 404
- [ ] 2分待ってリロードすると、一覧が「公開中」に変わり、公開ページが 200 になる

> ここで「一覧は公開中なのに公開ページは404」等の食い違いが出たら、
> `config.php` の `CMS_TIMEZONE` を確認してください。

---

## 7. 作品一覧の操作

`{BASE}/admin/index.php` で実施します。

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 7-1 | 検索ボックスにタイトルの一部を入力 | 該当作品だけが表示され、件数が「「〜」で N 件」になる | [ ] |
| 7-2 | 検索ボックスに制作期間の一部（例 `2026`）を入力 | 制作期間でも検索できる | [ ] |
| 7-3 | 検索中の画面 | **↑↓ の並び替えボタンが消え**、下部に注意書きが出る | [ ] |
| 7-4 | 「クリア」をクリック | 全件表示に戻り、↑↓ が復活する | [ ] |
| 7-5 | 各行の「順番」の ↑ をクリック | **1つ上の作品と入れ替わる** | [ ] |
| 7-6 | ↓ をクリック | 1つ下と入れ替わる | [ ] |
| 7-7 | 一番上の行 | ↑ ボタンが表示されない | [ ] |
| 7-8 | 一番下の行 | ↓ ボタンが表示されない | [ ] |
| 7-9 | 並び替えた後に公開ページ `samples/works.php` を開く | 管理画面と**同じ順序**で並んでいる | [ ] |
| 7-10 | チェックボックスで複数選択 | 「N 件選択中」のカウントが増える | [ ] |
| 7-11 | ヘッダーのチェックボックス | 全行が一括で選択される | [ ] |
| 7-12 | 何も選ばずに一括操作「適用」 | 「対象の作品を選択してください。」の警告 | [ ] |
| 7-13 | 複数選択 → 一括「削除する」 | 確認ダイアログ → まとめて削除される | [ ] |
| 7-14 | 作品を21件以上作る | 20件/ページで分割される | [ ] |
| 7-15 | ページ数が6以上ある状態でページネーションを見る | **全ページ番号ではなく、前後2ページ + 先頭/末尾 + `…`** で表示される | [ ] |
| 7-16 | 各行の「プレビュー」リンク | 新規タブで `preview.php` が開く | [ ] |
| 7-17 | 公開中の作品の行 | 「公開ページ ↗」リンクが出る（下書きには出ない） | [ ] |

> **7-5 〜 7-9 は v1.x で完全に機能していなかった箇所です**（B-1）。
> 旧版は全作品の `sort_order` が 0 だったため、↑↓ を押しても何も起きませんでした。
> **ここが動かなかったら v2.0.0 の主要な修正が効いていません。**

---

## 8. カテゴリ管理

`{BASE}/admin/categories.php` で実施します。

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 8-1 | 管理画面のナビに「カテゴリ」がある | クリックでカテゴリ管理が開く | [ ] |
| 8-2 | カテゴリ名 `Web Design` で slug 欄を空のまま追加 | slug が `web-design` になる（**数字にならない**） | [ ] |
| 8-3 | カテゴリ名 `ブログ`（日本語のみ）で追加 | slug が `category` になる | [ ] |
| 8-4 | 8-2 と同じ名前でもう1件追加 | slug が `web-design-2` になる | [ ] |
| 8-5 | カテゴリ名 `ブログ2` や `2026`（**数字を含む日本語 / 数字のみ**）で追加 | slug が `2` や `2026` に**ならず** `category-N` になる | [ ] |
| 8-6 | slug 欄に `custom` と手入力して追加 | その slug が使われる | [ ] |
| 8-7 | 一覧の「編集」をクリック | その行が入力欄に変わる | [ ] |
| 8-8 | 名前と slug を変更して「保存」 | 変更が反映される | [ ] |
| 8-9 | 編集中に「中止」をクリック | 編集をやめて一覧に戻る | [ ] |
| 8-10 | 一覧の「作品数」列 | そのカテゴリに紐付く作品数が出る | [ ] |
| 8-11 | 作品が紐付いているカテゴリを削除 | カテゴリが消え、**作品自体は残る**（紐付けだけ解消） | [ ] |

> **8-1 は v1.x でナビにリンクが無く、URL直打ちでしか開けなかった箇所です**（C-1）。
> **8-2 は slug に採番IDが入っていた箇所です**（B-3）。旧版では `/category/web-design` のような
> URLが永久に作れませんでした。
> **8-7 〜 8-9 の編集機能は v2.0.0 で新規追加**です（旧版は追加と削除のみ）。

---

## 9. タグ管理

`{BASE}/admin/tags.php` で実施します。

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 9-1 | タグ一覧を開く | 作品編集で自動作成されたタグが並ぶ | [ ] |
| 9-2 | 「作品数」列 | そのタグが付いた作品数が出る | [ ] |
| 9-3 | 「編集」→ タグ名を変更して保存 | 名前が変わり、slug も作り直される | [ ] |
| 9-4 | 既存の別タグと**同じ名前**に変更しようとする | 「そのタグ名はすでに使われています。」で拒否 | [ ] |
| 9-5 | タグを削除 | タグが消え、**作品自体は残る**（紐付けだけ解消） | [ ] |

---

## 10. スキル管理

**v2.0.0 の新機能です。** `{BASE}/admin/skill.php` で実施します。

### 10.1 追加・編集

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 10-1 | ナビの「スキル」をクリック | スキル一覧が開く（最初は空） | [ ] |
| 10-2 | 「+ 新規追加」→ スキル名 `PHP`、カテゴリ `プログラミング` で追加 | 一覧の「プログラミング」グループに追加される | [ ] |
| 10-3 | 「期間・習熟度」に `使用歴3年` を入力して保存 | 一覧と公開ページに表示される | [ ] |
| 10-4 | 「説明」に複数行のテキストを入力して保存 | 保存され、公開ページで**改行が保持されて**表示される | [ ] |
| 10-5 | アイコン画像をアップロードして保存 | 一覧にアイコンのサムネイルが出る | [ ] |
| 10-6 | スキル名を空で保存 | 「スキル名は必須です。」で拒否される | [ ] |
| 10-7 | 「編集」からアイコンを差し替え | 新しい画像に変わり、古い画像が `uploads/` から消える | [ ] |
| 10-8 | 「この画像を削除する」にチェックして保存 | アイコンが外れ、一覧では「—」になる | [ ] |

### 10.2 カテゴリとグループ表示

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 10-9 | `デザイン` カテゴリのスキル（例 `Figma`）を追加 | 「デザイン」グループが新しく現れる | [ ] |
| 10-10 | `その他` カテゴリのスキル（例 `Git`）を追加 | 「その他」グループが現れる | [ ] |
| 10-11 | 一覧のグループの並び順 | **プログラミング → デザイン → その他** の順（`SKILL_CATEGORIES` 順） | [ ] |
| 10-12 | 既存スキルのカテゴリを変更して保存 | 別グループに移動する | [ ] |

### 10.3 並び替え

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 10-13 | 同じカテゴリにスキルを3つ以上作る | グループ内に3行以上並ぶ | [ ] |
| 10-14 | 2番目のスキルの ↑ をクリック | 1番目と入れ替わる | [ ] |
| 10-15 | ↓ をクリック | 1つ下と入れ替わる | [ ] |
| 10-16 | 各グループの先頭行 / 最終行 | ↑ / ↓ がそれぞれ表示されない | [ ] |
| 10-17 | **別カテゴリをまたいで**並び替えできないこと | ↑↓ は同じカテゴリ内でのみ動く | [ ] |
| 10-18 | 並び替え後に `{BASE}/samples/skill.php` を開く | 管理画面と同じ順序で並ぶ | [ ] |

### 10.4 削除

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 10-19 | スキルの「削除」をクリック | 確認ダイアログが出る | [ ] |
| 10-20 | 削除を実行 | 一覧から消え、**アイコン画像も `uploads/` から消える** | [ ] |
| 10-21 | グループのスキルを全部消す | そのグループの見出しごと消える | [ ] |

---

## 11. メディア・画像処理

`{BASE}/admin/media.php` で実施します。

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 11-1 | メディアライブラリを開く | 画像一覧（寸法・サイズ・使用状況）が表示される | [ ] |
| 11-2 | 横幅 1600px を超える画像をサムネイルとしてアップロード | 最大幅 1600px に自動リサイズされる | [ ] |
| 11-3 | `-thumb`（300px）変種が生成されている | メディア一覧のサムネイル表示に使われる | [ ] |
| 11-4 | 一覧に `-thumb` 変種**そのもの**が行として出ていない | 元画像だけが並ぶ | [ ] |
| 11-5 | PNG（透過）をアップロード | 透過が保持される | [ ] |
| 11-6 | 5MB を超えるファイルをアップロード | 「画像サイズは5MB以下にしてください」で拒否 | [ ] |
| 11-7 | 作品のサムネイルに使っている画像 | 使用状況が「作品: {タイトル}」と出る | [ ] |
| 11-8 | スキルのアイコンに使っている画像 | 使用状況が「スキル: {スキル名}」と出る | [ ] |
| 11-9 | 本文セクションに挿入した画像 | 使用状況が「本文: 作品の本文セクション」と出る | [ ] |
| 11-10 | どこにも使っていない画像 | 「未使用」と出る | [ ] |
| 11-11 | 個別の「削除」 | 画像と `-thumb` 変種が両方消える | [ ] |
| 11-12 | 「未使用 N 件をまとめて削除」 | 確認ダイアログ → 未使用画像だけが消え、**使用中は残る** | [ ] |
| 11-13 | 使用中の画像を削除した後、その作品の公開ページを開く | 画像が壊れる（想定内。警告文どおり） | [ ] |

> **11-8 / 11-9 / 11-12 は v2.0.0 の新機能です。**
> 旧版は作品サムネイルしか使用状況を見ておらず、一括削除もありませんでした。

---

## 12. サイト設定

`{BASE}/admin/settings.php` で実施します。

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 12-1 | サイト設定を開く | 6項目の編集画面が表示される | [ ] |
| 12-2 | サイト名を変更して保存 | **保存直後の画面に新しい値が表示されている** | [ ] |
| 12-3 | 12-2 の直後にページをリロード | 同じ新しい値が表示される | [ ] |
| 12-4 | 公開ページ `samples/works.php` を開く | ヘッダーとタイトルに新しいサイト名が反映される | [ ] |
| 12-5 | 「作品一覧の表示件数」を 3 に変更 | `samples/works.php` が3件ずつのページ送りになる | [ ] |
| 12-6 | 表示件数に 0 や負数を入れて保存 | 1以上に補正される | [ ] |
| 12-7 | 何も変更せずに保存 | 「変更はありませんでした。」と出る | [ ] |
| 12-8 | 「公開サイトのURL」に任意のURLを設定 | 管理画面ナビの「サイトを表示 ↗」がそのURLに飛ぶ | [ ] |
| 12-9 | 「公開作品URLのパターン」に `https://example.com/works/{slug}` を設定 | 一覧の「公開ページ ↗」がそのURL形式になる | [ ] |
| 12-10 | 12-9 を空に戻す | `samples/single.php?slug=...` に戻る | [ ] |

> **12-2 は v1.x で壊れていた箇所です**（B-2）。
> 旧版は「更新しました」と出るのにフォームには古い値が表示され、
> リロードするまで保存されたか分かりませんでした。

---

## 13. アカウント設定

`{BASE}/admin/account.php` で実施します。

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 13-1 | アカウント設定を開く | ユーザー名・メール・パスワード変更欄が表示される | [ ] |
| 13-2 | メールアドレスだけ変更して保存 | 保存される（パスワード入力は不要） | [ ] |
| 13-3 | 不正な形式のメール（例 `abc`）を入力 | ブラウザのバリデーションか「形式が正しくありません」で拒否 | [ ] |
| 13-4 | 現在のパスワードを**間違えて**新パスワードを入力 | 「現在のパスワードが正しくありません。」で拒否 | [ ] |
| 13-5 | 新パスワードを 7文字で入力 | 「8文字以上にしてください。」で拒否 | [ ] |
| 13-6 | 新パスワードを英字のみ（例 `abcdefgh`）で入力 | 「英字と数字を両方含めてください。」で拒否 | [ ] |
| 13-7 | 正しい現パスワード + 有効な新パスワードで変更 | 「パスワードを更新しました」と表示される | [ ] |
| 13-8 | 13-7 の直後、**ログアウトせずに**別の管理画面を開く | そのまま操作できる（セッションが維持されている） | [ ] |
| 13-9 | ログアウトして**新しい**パスワードでログイン | ログインできる | [ ] |
| 13-10 | ユーザー名を変更して保存 | 変更され、次回ログインから新しい名前を使う | [ ] |

---

## 14. 公開ページ

### 14.1 作品一覧（Works）

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 14-1 | `{BASE}/samples/works.php` | 公開中の作品がカードで並ぶ | [ ] |
| 14-2 | 下書き・予約公開の作品 | 一覧に**出てこない** | [ ] |
| 14-3 | カードの表示内容 | サムネイル・タグ・タイトル・制作期間・概要が出る | [ ] |
| 14-4 | サムネイルが無い作品のカード | グラデーションのプレースホルダが出る（レイアウトが崩れない） | [ ] |
| 14-5 | 上部のカテゴリフィルター | 「All」＋カテゴリ名のボタンが並ぶ | [ ] |
| 14-6 | カテゴリボタンをクリック | そのカテゴリの作品だけに絞られ、ボタンがハイライトされる | [ ] |
| 14-7 | 表示件数を超える作品数がある場合 | 下部にページ送りが出る | [ ] |
| 14-8 | カードをクリック | 作品詳細に遷移する | [ ] |

### 14.2 作品詳細（Single）

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 14-9 | `{BASE}/samples/single.php?slug={slug}` | 作品詳細が表示される | [ ] |
| 14-10 | `{BASE}/samples/single.php?id={ID}` | 同じ作品が表示される（IDでも引ける） | [ ] |
| 14-11 | 存在しない slug / ID | 404 ページが表示される | [ ] |
| 14-12 | メタ情報ブロック | 制作期間・種別・カテゴリ・使用技術・リンクが出る | [ ] |
| 14-13 | カテゴリのリンクをクリック | そのカテゴリで絞った作品一覧に飛ぶ | [ ] |
| 14-14 | 本文セクション | 見出しと本文が登録順に並ぶ | [ ] |
| 14-15 | 見出しが空のセクション | 見出しなしで本文だけ出る（空の `<h2>` が出ない） | [ ] |
| 14-16 | 外部リンクのリンク | 新規タブで開き、`rel="noopener noreferrer"` が付いている | [ ] |
| 14-17 | 「← 作品一覧へ」 | 一覧に戻る | [ ] |

### 14.3 スキル（Skills）

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 14-18 | `{BASE}/samples/skill.php` | カテゴリごとにグループ分けされたスキルが並ぶ | [ ] |
| 14-19 | グループの順序 | プログラミング → デザイン → その他 | [ ] |
| 14-20 | アイコンが無いスキル | スキル名の頭文字が代わりに表示される | [ ] |
| 14-21 | スキルが0件のとき | 「スキルはまだ登録されていません。」と出る | [ ] |

### 14.4 レスポンシブ

- [ ] ブラウザ幅を 375px 程度まで狭める（開発者ツールのデバイスモード）
- [ ] 作品一覧が1カラムになる
- [ ] 作品詳細のメタ情報が縦積みになる
- [ ] 動画プレイヤーが 16:9 を保ったまま縮む
- [ ] **横スクロールバーが出ない**
- [ ] 管理画面でもハンバーガーメニューが機能する

---

## 15. セキュリティ確認

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 15-1 | 管理画面のフォームで `_csrf` を改ざんして送信（開発者ツール） | 403「CSRF検証に失敗しました」 | [ ] |
| 15-2 | `_csrf` を削除して送信 | 同上 | [ ] |
| 15-3 | **ログインフォーム**で `_csrf` を改ざんして送信 | 403（v2.0.0 でログインにもCSRFを追加） | [ ] |
| 15-4 | 未ログインで `admin/` 配下の各PHPを直接開く | すべて `login.php` にリダイレクト | [ ] |
| 15-5 | 未ログインで `admin/upload-image.php` に POST | ログイン画面へリダイレクトされ、保存されない | [ ] |
| 15-6 | 拡張子を偽装した非画像（中身がテキストの `.jpg`） | MIME検証で拒否される | [ ] |
| 15-7 | `.php` ファイルを画像として投稿しようとする | 拒否される | [ ] |
| 15-8 | 作品タイトルに `<script>alert(1)</script>` を入れて保存・表示 | 実行されず、文字列として表示される | [ ] |
| 15-9 | セッションCookie の属性（開発者ツール → Cookies） | `HttpOnly` と `SameSite=Lax` が付いている | [ ] |
| 15-10 | `{BASE}/uploads/` をブラウザで開く | ディレクトリ一覧が出ない（またはアクセス拒否） | [ ] |

### 15.11 本文セクションのXSS（v2.0.0 の重点修正）

v1.x では**ここから管理画面の乗っ取りが成立していました**（A-1）。必ず確認してください。

- [ ] 作品の本文セクションに、CKEditor の **ソース編集か直接POST**で次を保存する:

  ```html
  <p>ふつうの本文</p></textarea><script>alert(1)</script>
  ```

- [ ] 保存後、その作品の**編集画面を開き直す**
- [ ] **アラートが出ない**
- [ ] ページのソースを表示し、`<textarea>` の中身が
      `&lt;/textarea&gt;&lt;script&gt;` とエスケープされていることを確認

> CKEditor 経由で入力しにくい場合は、curl や開発者ツールの fetch で
> `section_body[]` に直接この文字列を送ってください。

### 15.12 setup.php

- [ ] `setup.php` がまだプロジェクト直下にある場合、**テスト完了後に削除する**

```bash
rm setup.php
```

> ⚠️ 公開環境に残すと、第三者が管理者ユーザーを登録できます（README 手順7）。

### 15.13 DB接続エラー時の情報漏洩

- [ ] `config.php` の `DB_PASS` をわざと間違った値にする
- [ ] 適当なページを開く
- [ ] 「データベースに接続できませんでした。設定を確認してください。」だけが表示され、
      **ホスト名・DB名・ユーザー名が画面に出ない**
- [ ] `config.php` を元に戻す

---

## 16. v1.x からの移行確認

`migrations/v2.0.0.sql` の動作確認です。**v1.x のデータを持っている場合のみ**実施します。

### 16.1 事前準備

```bash
MYSQL=/Applications/MAMP/Library/bin/mysql80/bin/mysql
DUMP=/Applications/MAMP/Library/bin/mysql80/bin/mysqldump

# 必ずバックアップを取る
$DUMP -h 127.0.0.1 -P 8889 -uroot -proot takoyaki_test > backup-before-v2.sql

# 検証用にコピーを作って、そちらで先に試す
$MYSQL -h 127.0.0.1 -P 8889 -uroot -proot -e "CREATE DATABASE mig_test DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"
$MYSQL -h 127.0.0.1 -P 8889 -uroot -proot mig_test < backup-before-v2.sql
$MYSQL -h 127.0.0.1 -P 8889 -uroot -proot mig_test < migrations/v2.0.0.sql
```

### 16.2 確認項目

| # | 確認するSQL | 期待される結果 | 確認 |
|---|------------|----------------|------|
| 16-1 | `SHOW TABLES;` | `post_sections` と `skills` がある | [ ] |
| 16-2 | `SHOW TABLES;` | `post_meta` / `audit_logs` / `password_resets` が**無い** | [ ] |
| 16-3 | `SELECT COUNT(*) FROM post_sections;` | 旧 `posts.body` の件数ぶん行がある | [ ] |
| 16-4 | `SELECT post_id, LEFT(body,40) FROM post_sections;` | 旧本文がそのまま移行されている | [ ] |
| 16-5 | `SELECT id, sort_order FROM posts ORDER BY sort_order;` | **1, 2, 3... の連番**になっている（全部0ではない） | [ ] |
| 16-6 | `SHOW COLUMNS FROM posts;` | `period` / `type` / `external_url` / `video_url` がある | [ ] |
| 16-7 | `SHOW COLUMNS FROM posts;` | `body` / `author_id` / `deleted_at` が**無い** | [ ] |
| 16-8 | `SHOW COLUMNS FROM users;` | `role` が**無い** | [ ] |
| 16-9 | `SELECT id, slug FROM categories;` | 数字だけの slug が `cat-{id}` になっている | [ ] |
| 16-10 | <code>SELECT * FROM site_settings WHERE &#96;key&#96; IN ('active_theme','enabled_plugins');</code> | 0件（廃止した設定が消えている） | [ ] |

### 16.3 移行後の手当て

- [ ] **ゴミ箱に入っていた記事が完全に削除されている**ことを理解している（復元不可）
- [ ] `SELECT id, username FROM users;` で**複数ユーザーが残っていないか**確認する
      （ロールが無くなったため、残っていると全員が管理者になります）
- [ ] 不要なユーザーを削除する: `DELETE FROM users WHERE id <> {残すID};`
- [ ] カテゴリ slug（`cat-2` 等）を管理画面から意味のある名前に直す
- [ ] slug が NULL のままの作品を、編集画面で開いて保存し直す（保存時に採番されます）
- [ ] 検証が済んだら本番DBに適用し、`mig_test` を削除する

---

## 付録A: テストデータのリセット

クリーンな状態からテストしたい場合:

```bash
MYSQL=/Applications/MAMP/Library/bin/mysql80/bin/mysql

$MYSQL -h 127.0.0.1 -P 8889 -uroot -proot -e "DROP DATABASE IF EXISTS takoyaki_test; CREATE DATABASE takoyaki_test DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"
$MYSQL -h 127.0.0.1 -P 8889 -uroot -proot takoyaki_test < schema.sql

# その後ブラウザで管理者を再登録
#   {BASE}/setup.php
```

アップロード済み画像もリセットする場合:

```bash
# .gitkeep と .htaccess は残すこと
find uploads -type f ! -name '.gitkeep' ! -name '.htaccess' -delete
```

> ⚠️ `uploads/.htaccess` は PHP 実行を止めるためのファイルです。**消さないでください。**

---

## 付録B: 既知の注意点

- **CLI から DB につながらない** — MAMP MySQL はソケットの場所が標準と異なります。
  `-h 127.0.0.1 -P 8889` を付けるか、`--socket=/Applications/MAMP/tmp/mysql/mysql.sock` を指定してください。
  `mysql` コマンド自体も `/Applications/MAMP/Library/bin/mysql80/bin/` にあります。
- **パスワードを忘れた場合** — v2.0.0 にリセット機能はありません。
  `password_hash()` で作ったハッシュを `users.password` に直接書き込んでください。

  ```bash
  php -r 'echo password_hash("newpass123", PASSWORD_DEFAULT), "\n";'
  ```

- **本文HTMLはサニタイズされません** — 書き手＝管理者本人を信頼する設計です。
  15.11 で確認しているのは「**管理画面のフォームを壊せないこと**」であって、
  「本文に任意のHTMLを書けないこと」ではありません。
- **自動テストは未整備** — 本手順は手動確認用です。
  `config.php` の純粋関数（`sluggify` / `unique_slug` / `parse_datetime_local` /
  `video_embed_url` / `is_post_live`）は自動テストにしやすいので、
  `tests/run.php` の整備が今後の課題です（EMBEDDING.md の E-4）。

---

## 付録C: 回帰テスト（v2.0.0 で直した不具合）

v2.0.0 で修正した不具合の**再発チェック用**の短縮版です。
コードを変更したあとは、最低限ここだけ通してください。

| # | 不具合 | 確認方法 | 期待 | 確認 |
|---|--------|---------|------|------|
| R-1 | **本文の保存型XSS**（A-1） | 本文セクションに `</textarea><script>alert(1)</script>` を保存し、編集画面を開き直す | アラートが出ない | [ ] |
| R-2 | **並び替えが効かない**（B-1） | 作品一覧で ↑↓ をクリック | 隣と入れ替わる | [ ] |
| R-3 | 新規作品の sort_order | 作品を新規作成後 `SELECT id,sort_order FROM posts;` | 0 ではなく最大値+1 が入る | [ ] |
| R-4 | **設定保存後に旧値**（B-2） | サイト設定を変更して保存 | 保存直後の画面に新値が出る | [ ] |
| R-5 | **カテゴリ slug が数字**（B-3） | `Web Design` でカテゴリ追加 | slug が `web-design` | [ ] |
| R-6 | **slug 重複で保存不可**（B-4） | 同名タイトルの作品を2件作る | 2件目が `-2` 付きで保存される | [ ] |
| R-7 | **日本語タイトルの slug が NULL**（B-4） | 日本語のみのタイトルで保存 | slug が `work-{ID}` | [ ] |
| R-8 | **タイムゾーンずれ**（B-5） | 公開日時を1分後に設定 | 一覧の「予約」と公開ページの404が一致する | [ ] |
| R-9 | **published_at が未検証**（B-6） | ステータス「公開」のまま、開発者ツールで `published_at=garbage` を送信 | 「公開日時の形式が正しくありません。」 | [ ] |
| R-10 | **削除で画像が残る**（B-7） | サムネイル付き作品を削除 | `uploads/` から画像が消える | [ ] |
| R-11 | **カテゴリ管理に到達不能**（C-1） | 管理画面のナビを見る | 「カテゴリ」リンクがある | [ ] |
| R-12 | **カテゴリが1つしか選べない**（C-2） | 作品編集のカテゴリ欄 | チェックボックスで複数選べる | [ ] |
| R-13 | **DB接続情報の漏洩**（A-3） | `DB_PASS` を壊してページを開く | ホスト名・DB名が画面に出ない | [ ] |
| R-14 | **ログインにCSRFが無い**（A-5） | ログインフォームのソースを見る | `_csrf` の hidden がある | [ ] |
| R-15 | **h() の null 警告**（E-2） | 全画面を一通り開く | Deprecated / Warning が出ない | [ ] |

### PHPの警告をまとめて確認する

R-15 のように「警告が出ないこと」を確認するには、エラー表示を有効にしてから全画面を回ります。

```bash
# 開発サーバを警告表示ありで起動（プロジェクトルートで実行）
php -d display_errors=1 -d error_reporting=E_ALL \
    -d pdo_mysql.default_socket=/Applications/MAMP/tmp/mysql/mysql.sock \
    -S 127.0.0.1:8971 -t .
```

`http://127.0.0.1:8971/` で各画面を開き、画面にもコンソールにも
Warning / Notice / Deprecated が出ないことを確認してください。

> `SITE_URL` が `localhost:8888` のままなのでリンクは MAMP 側に飛びます。
> 各画面はURLを直接打って開いてください。
