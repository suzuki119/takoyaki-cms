# Takoyaki CMS 手動テスト手順書

対象バージョン: **v1.12.0**
最終更新: 2026-05-26

この手順書は、Takoyaki CMS の全機能をブラウザ上で一通り動作確認するためのチェックリストです。
上から順に実施することを想定しています（後半のテストが前半で作ったデータに依存します）。

---

## 0. 前提・準備

### 0.1 動作環境

| 項目 | 値 |
|------|-----|
| Webサーバ | MAMP Apache（ポート **8888**） |
| DB | MAMP MySQL（ポート **8889**） |
| ベースURL | `http://localhost:8888/takoyaki-cms` |
| 管理画面 | `http://localhost:8888/takoyaki-cms/admin/index.php` |
| DB名 | `takoyaki_test`（user: `root` / pass: `root`） |

> 以降、URLは `{BASE}` = `http://localhost:8888/takoyaki-cms` と表記します。

### 0.2 事前チェック（環境の起動）

- [ ] MAMP が起動しており、Apache と MySQL が緑（稼働中）になっている
- [ ] ブラウザで `{BASE}/login.php` を開くとログイン画面が表示される
- [ ] ブラウザで `{BASE}/samples/index.php` を開くと記事一覧（公開ページ）が表示される

> CLI から DB を直接確認したい場合は、ソケットではなく TCP 指定が必要です:
> `mysql -h 127.0.0.1 -P 8889 -u root -proot takoyaki_test`
> （MAMP の MySQL は標準の `localhost` ソケットを使わないため）

### 0.3 テスト用アカウント

- 管理者: `admin`（role = admin）※パスワードは setup.php 登録時に設定したもの
- テスト中に editor ロールのユーザーを別途作成します（後述 セクション11）

### 0.4 テストの記録方法

各項目の `[ ]` を実施したら `[x]` に変更し、結果が期待と異なる場合は末尾にメモを残してください。

---

## 1. 認証（ログイン / ログアウト）

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 1-1 | `{BASE}/login.php` を開き、誤ったパスワードでログイン | エラー表示、ログインできない | [ ] |
| 1-2 | 正しい `admin` の資格情報でログイン | `{BASE}/admin/index.php`（記事一覧）に遷移 | [ ] |
| 1-3 | ログイン状態で `{BASE}/admin/index.php` を直接開く | 記事一覧が表示される（再ログインを求められない） | [ ] |
| 1-4 | 右上「ログアウト」をクリック | ログイン画面に戻る | [ ] |
| 1-5 | ログアウト状態で `{BASE}/admin/index.php` を直接開く | `login.php` にリダイレクトされる | [ ] |

### 1.6 ログイン試行回数制限（レート制限）

- [ ] ログアウト状態で、同じユーザー名に対し**わざと5回**連続で誤パスワードを送信する
- [ ] 6回目は「試行回数が多すぎる」旨でブロックされる（約15分の制限）
- [ ] 別の正しいパスワードでも制限中はログインできないことを確認

> 制限を解除してテストを続けるには、`login_attempts` テーブルを空にします:
> `DELETE FROM login_attempts;`（TCP接続で実行）

---

## 2. パスワードリセット（メール）

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 2-1 | ログイン画面の「パスワードをお忘れですか？」→ `{BASE}/forgot-password.php` | 申請フォームが表示 | [ ] |
| 2-2 | 存在するユーザー名/メールで申請 | 「メールを送信しました」等の汎用メッセージ | [ ] |
| 2-3 | **存在しない**ユーザー名/メールで申請 | 2-2 と**同じ**メッセージ（存在有無を明かさない） | [ ] |

> MAMP（ローカル）では `mail()` が実送信されないことが多いです。
> リセットリンクを直接確認するには `password_resets` テーブルを見ます。
> ただしトークンは SHA-256 ハッシュで保存されているため、生トークンは取得できません。
> リンクの動作確認をしたい場合は、実送信できる環境（本番/共有サーバ）で行うか、
> 一時的に `forgot-password.php` 側で生成トークンをログ出力して確認してください（テスト後に必ず戻す）。

- [ ] `password_resets` テーブルに行が1件追加されている（申請ごと）

---

## 3. 記事の作成・編集・削除（CRUD）

### 3.1 新規作成

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 3-1 | 記事一覧で「新規作成」→ `{BASE}/admin/post-new.php` | 編集フォームが表示 | [ ] |
| 3-2 | タイトルを入力（slug 欄は空のまま）して保存 | slug がタイトルから自動生成される（日本語のみなら空） | [ ] |
| 3-3 | 本文を CKEditor で入力（太字・見出し・リスト等） | リッチテキストが反映される | [ ] |
| 3-4 | 抜粋（excerpt）を入力して保存 | 一覧/詳細に抜粋が反映 | [ ] |
| 3-5 | サムネイル画像をアップロードして保存 | 一覧にサムネイルが表示される | [ ] |
| 3-6 | ステータスを「下書き」で保存 | 一覧で「下書き」と表示される | [ ] |

### 3.2 編集・削除

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 3-7 | 記事一覧から記事を開き、タイトルを変更して保存 | 変更が反映される | [ ] |
| 3-8 | サムネイルを別画像に差し替えて保存 | 新画像に変わり、古い `-thumb` 変種が削除される | [ ] |
| 3-9 | 記事一覧の「ゴミ箱へ」をクリック | 一覧から消え、ゴミ箱件数バッジが+1（→ セクション9で詳細） | [ ] |

### 3.3 本文内画像アップロード（CKEditor 連携）

- [ ] 編集画面の CKEditor で画像挿入ボタンから画像をアップロード
- [ ] 本文中に画像が挿入され、`uploads/` に保存される
- [ ] 不正な拡張子/MIME のファイルは拒否される（→ セクション16）

---

## 4. 予約公開・プレビュー・公開判定

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 4-1 | 記事の公開日時（published_at）を**未来**に設定し「公開」で保存 | 一覧で「予約公開」＋日時が表示される | [ ] |
| 4-2 | 公開ページ `{BASE}/samples/single.php?id={予約記事のID}` を開く | まだ公開時刻前なので **404**（公開されない） | [ ] |
| 4-3 | published_at を**過去**に変更して保存 | 一覧で「公開中」になる | [ ] |
| 4-4 | 同じ記事を公開ページで開く | **200** で表示される | [ ] |
| 4-5 | 下書き記事を `{BASE}/preview.php?id={下書きのID}` で開く（ログイン中） | 実際の見た目でプレビュー表示される | [ ] |
| 4-6 | ログアウトして `preview.php?id=...` を開く | `login.php` にリダイレクト（プレビューは要ログイン） | [ ] |

> 既存テストデータ参考: 公開記事 = id 3（slug `hello-world-post`）, id 5（slug `scheduled-post`）。
> 下書き = id 2, 4, 7。公開ページで下書きid（例 id=4）を開くと 404 になるのが正しい挙動です。

---

## 5. カテゴリ管理

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 5-1 | `{BASE}/admin/categories.php` を開く | カテゴリ一覧が表示 | [ ] |
| 5-2 | 新しいカテゴリを追加 | 一覧に追加される | [ ] |
| 5-3 | 記事編集画面でカテゴリを複数選択して保存 | 多対多で紐付く | [ ] |
| 5-4 | 公開ページ `{BASE}/samples/category.php?id={カテゴリID}` | そのカテゴリの公開記事のみ表示 | [ ] |
| 5-5 | 記事が紐付いているカテゴリを削除 | カテゴリが消え、`post_categories` の紐付けも解消 | [ ] |

---

## 6. タグ

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 6-1 | 記事編集画面のタグ欄に `php, cms, test` のようにカンマ区切りで入力して保存 | 未登録タグが自動作成される | [ ] |
| 6-2 | `{BASE}/admin/tags.php` を開く | 作成されたタグが一覧表示される | [ ] |
| 6-3 | 別の記事に同じタグ名を付与 | 既存タグが再利用される（重複作成されない） | [ ] |
| 6-4 | `admin/tags.php` でタグを削除 | タグと記事の紐付けが解消される | [ ] |
| 6-5 | 公開ページ `single.php` でタグが表示される（samplesの例） | タグが表示される | [ ] |

---

## 7. カスタムフィールド（post_meta）

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 7-1 | 記事編集画面で「+ フィールドを追加」 | key/value 入力行が追加される | [ ] |
| 7-2 | key と value を入力して保存 | `post_meta` に保存される | [ ] |
| 7-3 | 同じ key を複数行追加して保存 | 配列として複数値が保存される | [ ] |
| 7-4 | フィールドを削除して保存 | 該当メタが削除される | [ ] |
| 7-5 | `preview.php` / `samples/single.php` でカスタムフィールドが表示される | 値が表示される | [ ] |

---

## 8. メディア・画像処理

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 8-1 | `{BASE}/admin/media.php` を開く（**admin限定**） | アップロード済み画像の一覧（寸法・サイズ・使用状況） | [ ] |
| 8-2 | 横幅 1600px を超える大きい画像をサムネイルとしてアップロード | 最大幅 1600px に自動リサイズされる | [ ] |
| 8-3 | 同じ画像の `-thumb`（300px）変種が生成されている | media に thumb 変種が確認できる | [ ] |
| 8-4 | PNG（透過）をアップロード | 透過が保持される | [ ] |
| 8-5 | 5MB を超えるファイルをアップロード | サイズ超過で拒否される | [ ] |
| 8-6 | media から未使用画像を削除 | ファイルが削除され、一覧から消える | [ ] |

---

## 9. ゴミ箱（ソフトデリート）

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 9-1 | 記事一覧で「ゴミ箱へ」 | 一覧から消え、ゴミ箱バッジ件数が増える | [ ] |
| 9-2 | `{BASE}/admin/index.php?view=trash` を開く | ゴミ箱内の記事一覧が表示 | [ ] |
| 9-3 | 公開ページ（samples / feed / sitemap）にゴミ箱記事が出ない | 表示されない | [ ] |
| 9-4 | ゴミ箱で「復元」 | 通常一覧に戻る | [ ] |
| 9-5 | ゴミ箱で「完全に削除」 | DBから物理削除される（復元不可） | [ ] |

---

## 10. 一覧の操作（検索・ページネーション・一括・並び替え）

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 10-1 | 記事一覧の検索ボックスにキーワード → `?q=...` | タイトル/本文の LIKE 検索結果が表示 | [ ] |
| 10-2 | 記事を21件以上作り、ページネーションを確認 | 20件/ページで分割される | [ ] |
| 10-3 | チェックボックスで複数選択 →「ゴミ箱へ移動」 | 選択記事がまとめてゴミ箱へ | [ ] |
| 10-4 | ゴミ箱で複数選択 →「復元」/「完全に削除」 | 一括で復元/削除される | [ ] |
| 10-5 | 通常一覧で「順番」の ↑ / ↓ ボタン | 隣の記事と sort_order が入れ替わる | [ ] |
| 10-6 | 各行の「プレビュー」リンク | 新規タブで `preview.php` が開く | [ ] |

---

## 11. ユーザー管理・ロール（admin / editor）

### 11.1 ユーザー管理（admin で実施）

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 11-1 | `{BASE}/admin/users.php` を開く（**admin限定**） | ユーザー一覧が表示 | [ ] |
| 11-2 | role = `editor` の新規ユーザーを追加 | 一覧に追加される | [ ] |
| 11-3 | `{BASE}/admin/user-edit.php` で editor のパスワードをリセット | 新パスワードでログインできる | [ ] |
| 11-4 | 最後の admin を削除/降格しようとする | ブロックされる（自己ロックアウト防止） | [ ] |
| 11-5 | 自分自身を削除しようとする | ブロックされる | [ ] |

### 11.2 editor 権限の確認（editor でログインし直して実施）

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 11-6 | editor で `admin/users.php` を開く | アクセス拒否（admin限定） | [ ] |
| 11-7 | editor で `admin/media.php` / `settings.php` / `logs.php` / `backup.php` / `plugins.php` | いずれもアクセス拒否 | [ ] |
| 11-8 | editor で記事の作成・編集・カテゴリ・タグ操作 | 操作できる | [ ] |
| 11-9 | editor で `admin/account.php`（自分の設定） | アクセスできる | [ ] |

---

## 12. アカウント設定

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 12-1 | `{BASE}/admin/account.php` を開く | 自分のアカウント設定画面 | [ ] |
| 12-2 | 現在のパスワードを**間違えて**メール/パスワード変更 | 拒否される | [ ] |
| 12-3 | 現在のパスワードを正しく入力して変更 | 変更が反映される（次回ログインで新値が有効） | [ ] |

---

## 13. サイト設定

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 13-1 | `{BASE}/admin/settings.php` を開く（**admin限定**） | サイト名/説明/フッター/表示件数の編集画面 | [ ] |
| 13-2 | サイト名を変更して保存 | `samples/index.php` のタイトルに反映される | [ ] |
| 13-3 | 表示件数を変更 | 公開一覧の表示件数が変わる | [ ] |

---

## 14. 監査ログ

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 14-1 | これまでの操作後に `{BASE}/admin/logs.php` を開く（**admin限定**） | 操作履歴が新しい順に表示される | [ ] |
| 14-2 | 記事 trash / restore / delete のアクション名を確認 | `post.trash` / `post.restore` / `post.delete` 等が記録 | [ ] |
| 14-3 | ログのページネーション | 件数が多いとページ分割される | [ ] |

---

## 15. バックアップ・プラグイン

### 15.1 DBバックアップ

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 15-1 | `{BASE}/admin/backup.php` を開く（**admin限定**） | バックアップ画面が表示 | [ ] |
| 15-2 | ダウンロード実行 | `.sql` がダウンロードされ、CREATE TABLE + INSERT を含む | [ ] |
| 15-3 | ダウンロードが audit_logs に記録される | `backup` 系アクションが記録 | [ ] |

### 15.2 プラグイン・ショートコード・フック

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 15-4 | `{BASE}/admin/plugins.php` を開く（**admin限定**） | `hello-world` がリストに表示 | [ ] |
| 15-5 | `hello-world` を有効化 | チェックが入り、有効になる | [ ] |
| 15-6 | 記事本文に hello-world のショートコード（例 `[hello]`）を入れて公開 | `samples/single.php` で展開表示される | [ ] |
| 15-7 | プラグインを無効化 | ショートコードが展開されなくなる | [ ] |

> プラグインの具体的なショートコード/フック名は `plugins/hello-world/index.php` を参照。

---

## 16. セキュリティ確認

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 16-1 | 管理画面のフォームで、`_csrf` を改ざん/削除して送信（開発者ツール） | 403「CSRF検証に失敗しました」 | [ ] |
| 16-2 | 未ログインで各 `admin/*.php` を直接開く | すべて `login.php` にリダイレクト | [ ] |
| 16-3 | 画像アップロードで拡張子を偽装した非画像ファイル（中身がテキストの `.jpg` 等） | MIME 検証で拒否される | [ ] |
| 16-4 | 出力エスケープ: 記事タイトルに `<script>alert(1)</script>` を入れて保存・表示 | スクリプトが実行されず、文字列として表示される | [ ] |
| 16-5 | セッションCookie の属性確認（開発者ツール → Cookies） | `HttpOnly` と `SameSite=Lax` が付与されている | [ ] |
| 16-6 | **`setup.php` の扱い** | 本番では削除必須（→ 下記注意） | [ ] |

> ⚠️ **`setup.php` が現在もプロジェクト直下に残っています。**
> ローカルテスト中は問題ありませんが、公開環境では第三者が管理者を上書き登録できるため、
> テスト完了後・本番設置前に必ず `rm setup.php` してください（README の手順7）。

---

## 17. フロントエンド（公開側）

| # | 手順 | 期待される結果 | 確認 |
|---|------|----------------|------|
| 17-1 | `{BASE}/samples/index.php` | 公開記事の一覧（サイト設定の件数分） | [ ] |
| 17-2 | `{BASE}/samples/single.php?id={公開記事ID}` または `?slug=...` | 記事詳細が表示 | [ ] |
| 17-3 | `{BASE}/samples/single.php?id={下書きID}` | 404（公開されない） | [ ] |
| 17-4 | `{BASE}/samples/category.php?id={カテゴリID}` | カテゴリ別一覧 | [ ] |
| 17-5 | `{BASE}/feed.php` | RSS 2.0 XML（最新50件、Content-Type: application/rss+xml） | [ ] |
| 17-6 | `{BASE}/sitemap.php` | XML サイトマップ | [ ] |

### 17.7 きれいなURL（任意 / オプトイン）

`.htaccess` を有効化した場合のみ実施:

- [ ] `cp .htaccess.example .htaccess` し、`RewriteBase` を `/takoyaki-cms/` に合わせる
- [ ] `{BASE}/` でトップ（記事一覧）が表示される
- [ ] `{BASE}/post/{slug}` で記事詳細が表示される
- [ ] `{BASE}/category/{slug}` / `{BASE}/tag/{slug}` が表示される
- [ ] mod_rewrite 無効環境向け `{BASE}/router.php?p=/post/{slug}` でも動作する

> 既存テストデータの slug は数値（カテゴリ slug = `2`, `3`）や `hello-world-post` などです。
> きれいなURLをきちんと試すなら、英数字 slug の記事/カテゴリを用意すると確認しやすいです。

---

## 付録A: テストデータのリセット

クリーンな状態からテストしたい場合（TCP 接続で実行）:

```bash
# DBを作り直す
mysql -h 127.0.0.1 -P 8889 -u root -proot -e "DROP DATABASE IF EXISTS takoyaki_test; CREATE DATABASE takoyaki_test DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"
mysql -h 127.0.0.1 -P 8889 -u root -proot takoyaki_test < schema.sql

# その後ブラウザで setup.php にアクセスして管理者を再登録
#   {BASE}/setup.php
```

> アップロード済み画像をリセットする場合は `uploads/` 配下の画像ファイルも削除してください
> （`.gitkeep` は残す）。

## 付録B: 既知の注意点

- **CLI から DB に直接つながらない** — MAMP MySQL はソケットが標準と異なるため、
  `mysql`/`php` を CLI で使う場合は `-h 127.0.0.1 -P 8889`（PHP は DSN に `host=127.0.0.1;port=8889`）を指定。
- **メール送信** — ローカルでは `mail()` が実送信されないことがある（パスワードリセットのリンク確認に注意）。
- **自動テストは未整備** — 本手順は手動確認用。CI 化したい場合は別途 PHPUnit 等の導入が必要。
